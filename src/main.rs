// mod config;

// use config::*;

fn main() {
    // let app_config = get_config();
    // if let Some(uri) = app_config.uri.as_deref() {
    //     println!("DB URI: {uri}");
    // }

    api::main();
}
